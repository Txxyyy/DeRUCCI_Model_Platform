# 慕思通用物模型管理平台 PRD

**Date**: 2026-03-05
**Author**: 产品团队
**Status**: Draft
**Version**: 1.1

---

## 1. Executive Summary

### 1.1 问题陈述

随着慕思智能化战略的推进，越来越多的智能产品（智能床垫、智能枕头、智能睡眠监测设备等）需要接入统一的管理平台。当前的设备接入缺乏标准化管理，导致：
- 每款产品都需要单独开发接入协议
- 设备数据格式不统一，难以统一分析
- 新产品接入周期长，成本高

### 1.2 解决方案

建设一个**通用物模型管理平台**，实现产品的标准化管理和快速接入。平台类比华为DP平台、涂鸦设备开发平台，提供：
- 产品信息的统一管理
- 物模型的标准化定义（由产品经理维护）
- 设备的快速接入
- 固件OTA升级

### 1.3 业务价值

| 价值 | 说明 |
|-----|------|
| **标准化** | 统一的物模型规范，设备数据格式标准化 |
| **快速接入** | 基于模板快速创建新产品的物模型，缩短接入周期 |
| **统一管理** | 集中管理所有产品、设备和固件版本 |
| **可扩展** | 支持多种协议和设备类型 |

### 1.4 时间规划

| 阶段 | 周次 | 主要交付物 |
|-----|------|-----------|
| Phase 1 | 2周 | 产品创建、产品列表、产品详情页基本功能 |
| Phase 2 | 2周 | 物模型管理：模板选择、功能点增删改 |
| Phase 3 | 1周 | 物模型JSON导出功能 |
| Phase 4 | 2周 | 设备接入基础功能 |
| Phase 5 | 1周 | OTA基础功能 |
| Phase 6 | 1周 | 测试优化，Beta发布 |

---

## 2. Problem Definition

### 2.1 当前问题

| 问题 | 影响 | 严重程度 |
|-----|------|---------|
| 每款产品单独开发接入协议 | 开发重复工作 | 高 |
| 设备数据格式不统一 | 数据分析困难 | 高 |
| 新产品接入周期长 | 上市时间延迟 | 高 |
| 缺乏标准物模型模板 | 产品经理无法快速定义 | 中 |

### 2.2 目标用户

| 用户角色 | 使用场景 | 需求 |
|---------|---------|------|
| **产品经理** | 创建产品、定义物模型、管理产品信息 | **维护品类物模型模板** |
| 研发工程师 | 查看物模型规范，开发设备端固件、APP开发 | 获取物模型JSON |
| 运维人员 | 监控设备状态、管理OTA升级 | 设备管理 |
| 测试工程师 | 验证物模型功能、测试设备接入 | 测试用例 |

### 2.3 关键角色说明

**产品经理**是品类物模型模板的维护者：
- 在「品类物模型」页面创建和维护各品类的标准物模型模板
- 创建产品时，选择对应品类的模板
- 模板包含属性、事件、命令的定义

---

## 3. Solution Overview

### 3.1 产品定位

慕思内部使用的物联网设备管理平台，类比：
- 华为DP平台
- 涂鸦设备开发平台

核心能力：
1. 产品管理（创建、列表、详情）
2. 物模型管理（功能点、模板、版本、导出）
3. 设备接入（注册、认证、消息路由）
4. OTA管理（固件、升级任务）

### 3.2 MVP定义

**MVP范围**：
- ✅ 产品管理（创建、列表、详情）
- ✅ 物模型管理（功能点、模板、版本、导出）
- ⬜ 设备接入（后续阶段）
- ⬜ OTA管理（后续阶段）

**成功标准**：
- 产品经理能够创建和维护品类物模型模板
- 研发工程师能够导出物模型JSON用于开发
- 产品创建流程在5分钟内完成

### 3.3 Out of Scope

本次MVP不考虑：
- 设备实际接入（仅物模型定义）
- 设备监控实时数据
- OTA差分升级
- 多语言支持

---

## 4. User Stories & Requirements

### 4.1 用户故事

#### 产品经理

```
作为产品经理
我想维护品类物模型模板
以便快速创建新产品时选择标准模板

验收标准：
- [ ] 可以创建物模型模板
- [ ] 可以为模板添加属性/事件/命令
- [ ] 可以编辑和删除模板
- [ ] 可以查看模板列表
```

```
作为产品经理
我想创建新产品并选择物模型模板
以便快速定义产品能力

验收标准：
- [ ] 可以填写产品基本信息（名称、型号、品牌、品类）
- [ ] 系统自动生成PID
- [ ] 可以选择通信方式（MQTT/BLE）
- [ ] 可以选择物模型模板
- [ ] 可以查看产品列表
```

#### 研发工程师

```
作为研发工程师
我想导出物模型JSON
以便开发APP和固件

验收标准：
- [ ] 可以导出物模型JSON文件
- [ ] JSON格式符合标准规范
- [ ] 包含完整的属性、事件、命令定义
```

### 4.2 功能需求

#### 4.2.1 产品管理

| 功能 | 优先级 | 说明 |
|-----|-------|------|
| 产品创建 | P0 | 创建新产品，自动生成PID |
| 产品列表 | P0 | 卡片展示、筛选、搜索 |
| 产品详情 | P0 | 查看和编辑产品信息 |
| 产品删除 | P1 | 仅草稿状态可删除 |
| 产品发布 | P1 | 发布产品到已发布状态 |

#### 4.2.2 物模型管理

| 功能 | 优先级 | 说明 |
|-----|-------|------|
| 功能点管理 | P0 | 添加/编辑/删除属性、事件、命令 |
| 数据类型 | P0 | 支持int/float/bool/string/enum |
| 取值范围 | P0 | 数值类型支持min/max |
| 枚举值 | P0 | enum类型支持枚举选项 |
| 版本管理 | P1 | 物模型多版本管理 |
| JSON导出 | P0 | 导出物模型用于开发 |

#### 4.2.3 品类物模型管理（产品经理角色）

| 功能 | 优先级 | 说明 |
|-----|-------|------|
| 创建模板 | P0 | 创建品类物模型模板 |
| 模板列表 | P0 | 查看所有模板 |
| 编辑模板 | P0 | 编辑模板的功能点 |
| 删除模板 | P1 | 删除模板 |
| 模板选择 | P0 | 创建产品时选择模板 |

### 4.3 非功能需求

| 需求 | 标准 |
|-----|------|
| 性能 | 页面加载 < 2秒 |
| 可用性 | 支持主流浏览器 |
| 安全性 | 权限控制、数据校验 |
| 可扩展性 | 模块化设计 |

---

## 5. Design & User Experience

### 5.1 页面结构

```
┌─────────────────────────────────────────────────────────────────────┐
│  顶部导航栏 (Header)                                                  │
│  [Logo]  产品管理  物模型  设备管理  OTA升级  系统设置     [用户头像] │
├────────────┬────────────────────────────────────────────────────────┤
│            │                                                         │
│  左侧菜单   │                    主内容区                            │
│  (Sidebar) │                  (Main Content)                       │
│            │                                                         │
│  - 产品管理 │                                                         │
│    - 产品列表                                                         │
│    - 产品详情                                                         │
│  - 物模型  │                                                         │
│    - 品类物模型 ⭐(产品经理)                                          │
│    - 我的模型                                                         │
│  - 设备    │                                                         │
│  - OTA     │                                                         │
│            │                                                         │
└────────────┴────────────────────────────────────────────────────────┘
```

### 5.2 核心页面

| 页面 | 说明 |
|-----|------|
| 产品列表 | 卡片展示、筛选（品类、通信方式、状态）、搜索 |
| 产品创建/编辑 | 表单（名称、型号、品牌、品类、通信方式） |
| 产品详情 | 基本信息 + 物模型管理面板 |
| 品类物模型 | 模板列表、创建/编辑模板 |
| 物模型详情 | 功能点管理（属性/事件/命令 Tab切换） |

### 5.3 色彩系统

参考 `docs/Design/UI_UX设计规范_ProMax.md`：

| 用途 | 色值 |
|-----|------|
| 主色 | #2563EB |
| 成功 | #10B981 |
| 警告 | #F59E0B |
| 错误 | #EF4444 |
| 文字主色 | #0F172A |
| 背景色 | #F8FAFC |

---

## 6. Technical Specifications

### 6.1 技术栈

| 层级 | 技术 |
|-----|------|
| 前端 | Vue 3 + Element Plus |
| 后端 | Spring Boot (微服务) |
| 数据库 | MySQL + Redis |
| 消息队列 | Kafka |
| 协议 | MQTT, BLE |

### 6.2 数据模型

#### 产品实体

```java
@Entity
@Table(name = "t_product")
class Product {
    String pid;          // PID_xxxxxx 系统生成
    String name;        // 产品名称
    String model;       // 产品型号 DR-M001
    String brand;       // 产品品牌
    String category;    // 品类：智能床垫/电动床/智能枕头
    String protocol;    // MQTT/BLE
    String status;      // DRAFT/PUBLISHED
}
```

#### 物模型实体

```java
@Entity
@Table(name = "t_thing_model")
class ThingModel {
    Long productId;
    String version;     // v1.0.0
    String propertiesJson;
    String eventsJson;
    String commandsJson;
    String status;      // DRAFT/PUBLISHED
}
```

#### 物模型功能点

```java
@Entity
@Table(name = "t_thing_model_point")
class ThingModelPoint {
    Long thingModelId;
    String pointId;     // 物模型ID
    String name;
    String pointType;   // PROPERTY/EVENT/COMMAND
    String dataType;    // int/float/bool/string/enum
    String access;      // readOnly/readWrite
    String unit;
    String rangeJson;   // 取值范围
    String enumValuesJson;
}
```

#### 物模型模板（产品经理维护）

```java
@Entity
@Table(name = "t_thing_model_template")
class ThingModelTemplate {
    String name;
    String code;        // 模板编码
    String category;    // 所属品类
    String propertiesJson;
    String eventsJson;
    String commandsJson;
    Boolean system;    // false（用户创建）
}
```

### 6.3 API设计

| 接口 | 方法 | 说明 |
|-----|------|------|
| /api/products | GET | 产品列表 |
| /api/products | POST | 创建产品 |
| /api/products/{id} | GET | 产品详情 |
| /api/products/{id} | PUT | 更新产品 |
| /api/thing-model/templates | GET | 模板列表 |
| /api/thing-model/templates | POST | 创建模板 |
| /api/products/{id}/thing-model | GET | 物模型详情 |
| /api/products/{id}/thing-model/export | GET | 导出JSON |

---

## 7. Go-to-Market Strategy

### 7.1 发布计划

| 阶段 | 内容 | 目标用户 |
|-----|------|---------|
| Beta | 产品管理 + 物模型管理 | 内部团队 |
| v1.0 | 完整MVP | 内部用户 |

### 7.2 验收标准

详见第8节

---

## 8. Risks & Mitigations

| 风险 | 概率 | 影响 | 缓解策略 |
|-----|------|------|---------|
| 需求变更 | 高 | 中 | 敏捷迭代，定期确认 |
| 技术难度 | 中 | 中 | 技术预研 |
| 资源不足 | 中 | 高 | 预留buffer |

---

## 9. Timeline & Milestones

| Milestone | Week | Deliverables |
|-----------|------|--------------|
| PRD评审 | Week 1 | PRD定稿 |
| 设计完成 | Week 2 | UI设计 |
| Phase 1 | Week 1-2 | 产品管理基本功能 |
| Phase 2 | Week 3-4 | 物模型管理 |
| Phase 3 | Week 5 | 导出功能 |
| Beta发布 | Week 7 | 内部测试 |

---

## 10. Open Questions

1. 物模型模板是否需要版本管理？
2. 是否需要支持模板导入/导出？
3. 设备接入的具体协议细节？

---

*最后更新: 2026-03-05*
